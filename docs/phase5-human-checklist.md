# Phase 5 — human / off-site checklist (not done by Cursor)

## Done in repo
- [x] Organization entity schema extended (`index.html`)
- [x] Service nodes on `/for-businesses` and `/for-creators`
- [x] Canonical description in `BRAND.md` + homepage meta + visible definitional block
- [x] Crawler access audit (live): `robots.txt` allows all bots including AI; blog HTML is server-rendered (`<h1>` present in curl)

## Owner decisions still needed
1. **Sign-off** the `BRAND.md` canonical description before pasting to LinkedIn / Facebook / Crunchbase.
2. **Go / no-go** on Helsinki nano-creator first-party benchmark (§6.4) — GDPR + sample size.
3. **Who owns Reddit** participation (§6.2) — real person, ongoing.
4. Off-site profile completeness: GBP (if eligible), LinkedIn, Crunchbase, Product Hunt, Finnish directories — same description everywhere.
5. GEO prompt-set baseline (20–30 prompts × 3 runs) before measuring post-change citation lift.
6. Real photo for Andrey Shepelev (currently initials avatar at `assets/authors/andrey-shepelev.png`).
7. Confirm whether Andrey is a company founder (founder field left out of Organization schema until confirmed).
8. Blog nav: keep in header vs move to footer once more pages exist — no action yet.
