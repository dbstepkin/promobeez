// AUTO-GENERATED by npm run build:landing-attribution. Do not edit.
"use strict";
(() => {
  var __defProp = Object.defineProperty;
  var __getOwnPropSymbols = Object.getOwnPropertySymbols;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __propIsEnum = Object.prototype.propertyIsEnumerable;
  var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
  var __spreadValues = (a, b) => {
    for (var prop in b || (b = {}))
      if (__hasOwnProp.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    if (__getOwnPropSymbols)
      for (var prop of __getOwnPropSymbols(b)) {
        if (__propIsEnum.call(b, prop))
          __defNormalProp(a, prop, b[prop]);
      }
    return a;
  };

  // ../supabase/functions/_shared/signup-attribution-core.ts
  var SIGNUP_ATTRIBUTION_COOKIE = "pb_attribution";
  var SIGNUP_ATTRIBUTION_COOKIE_MAX_AGE = 60 * 60 * 24 * 30;
  function hostnameFromReferrer(referrer) {
    if (!(referrer == null ? void 0 : referrer.trim())) return null;
    try {
      return new URL(referrer).hostname.toLowerCase();
    } catch (e) {
      return null;
    }
  }
  function isGoogleHost(hostname) {
    return hostname === "google.com" || hostname.endsWith(".google.com");
  }
  function isFacebookHost(hostname) {
    return hostname === "facebook.com" || hostname.endsWith(".facebook.com") || hostname === "fb.com" || hostname.endsWith(".fb.com");
  }
  function isInstagramHost(hostname) {
    return hostname === "instagram.com" || hostname.endsWith(".instagram.com");
  }
  function resolveSignupAttribution(input) {
    var _a, _b, _c, _d, _e;
    const url = new URL(input.href);
    const fbclid = ((_a = url.searchParams.get("fbclid")) == null ? void 0 : _a.trim()) || void 0;
    const gclid = ((_b = url.searchParams.get("gclid")) == null ? void 0 : _b.trim()) || void 0;
    const msclkid = ((_c = url.searchParams.get("msclkid")) == null ? void 0 : _c.trim()) || void 0;
    let source;
    let medium = null;
    if (fbclid) {
      source = "facebook_ads";
      medium = "paid";
    } else if (gclid) {
      source = "google_ads";
      medium = "paid";
    } else if (msclkid) {
      source = "microsoft_ads";
      medium = "paid";
    } else {
      const refHost = hostnameFromReferrer(input.referrer);
      const ownHost = url.hostname.toLowerCase();
      if (!refHost || refHost === ownHost) {
        source = "direct";
      } else if (isGoogleHost(refHost)) {
        source = "google_organic";
        medium = "organic";
      } else if (isFacebookHost(refHost)) {
        source = "facebook_social";
        medium = "social";
      } else if (isInstagramHost(refHost)) {
        source = "instagram_social";
        medium = "social";
      } else {
        source = "referral";
        medium = refHost;
      }
    }
    const clickIds = {};
    if (fbclid) clickIds.fbclid = fbclid;
    if (gclid) clickIds.gclid = gclid;
    if (msclkid) clickIds.msclkid = msclkid;
    return __spreadValues({
      source,
      medium,
      referrer: ((_d = input.referrer) == null ? void 0 : _d.trim()) || null,
      landing_path: (_e = input.pathname) != null ? _e : url.pathname,
      captured_at: (/* @__PURE__ */ new Date()).toISOString()
    }, Object.keys(clickIds).length > 0 ? { click_ids: clickIds } : {});
  }

  // src/lib/signup-attribution-landing-entry.ts
  function promobeezCookieDomainPart() {
    const hostname = window.location.hostname;
    const isPromobeezHost = hostname === "promobeez.com" || hostname.endsWith(".promobeez.com");
    return isPromobeezHost ? "; domain=.promobeez.com" : "";
  }
  function secureCookiePart() {
    return window.location.protocol === "https:" ? "; Secure" : "";
  }
  function readCookie() {
    const prefix = `${SIGNUP_ATTRIBUTION_COOKIE}=`;
    for (const part of document.cookie.split(";")) {
      const trimmed = part.trim();
      if (trimmed.startsWith(prefix)) {
        try {
          return JSON.parse(decodeURIComponent(trimmed.slice(prefix.length)));
        } catch (e) {
          return null;
        }
      }
    }
    return null;
  }
  function setCookie(payload) {
    const encoded = encodeURIComponent(JSON.stringify(payload));
    document.cookie = `${SIGNUP_ATTRIBUTION_COOKIE}=${encoded}; path=/; max-age=${SIGNUP_ATTRIBUTION_COOKIE_MAX_AGE}; SameSite=Lax${promobeezCookieDomainPart()}${secureCookiePart()}`;
  }
  if (!readCookie()) {
    setCookie(
      resolveSignupAttribution({
        href: window.location.href,
        referrer: document.referrer || null,
        pathname: window.location.pathname
      })
    );
  }
})();
